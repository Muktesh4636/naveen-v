{
  "name": "visaslots-extension",
  "version": "2.0.8",
  "private": true,
  "description": "Chrome extension rebuilt from the shipped VisaSlots.info 2.0.2 package",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "check": "node build.mjs --check"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
